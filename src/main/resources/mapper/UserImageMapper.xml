<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.resume.dao.UserImageDAO">
    <select id="findImageById" parameterType="String" resultType="UserImageDTO">
        select keyvalue,url
        from userimage
        where userid = #{userid}
    </select>
    <update id="updateImage" parameterType="AwsS3" >
        update userimage
        set keyvalue =#{keyvalue}, url =#{url}
        where userid='rudnf9605'
    </update>
</mapper>